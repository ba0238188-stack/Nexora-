# Nexora — online APK build

This project includes a GitHub Actions workflow at `.github/workflows/build-apk.yml`.

After uploading the project to a GitHub repository, open **Actions → Build Nexora APK → Run workflow** (or push to `main`).
When the build finishes, open the workflow run and download the **Nexora-debug-apk** artifact.
